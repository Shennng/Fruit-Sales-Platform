<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 全局setting配置，根据需要添加 -->
    <!-- 配置别名 -->
    <typeAliases>
        <!-- 批量扫描别名 -->
        <package name="com.fruitsalesplatform.entity"/>
    </typeAliases>
    <!-- 数据库连接在spirng MVC中配置 -->
    <!-- 配置mapper
        由于使用Spirng和Mybatis的整合包进行Mapper扫描，这里不需要配置了
        但必须遵循mapper.xml(mapper SQL语句)和mapper.java(接口类)同名且在一个目录下 -->
    <!--<mappers></mappers>-->
</configuration>